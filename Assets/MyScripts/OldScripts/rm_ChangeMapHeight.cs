using System;
using System.Collections;
using System.Collections.Generic;
using Mapbox.Unity.Map;
using Unity.XR.CoreUtils;
using UnityEngine;
using UnityEngine.UI;

public class rm_ChangeMapHeight : MonoBehaviour
{

    /*[SerializeField] Slider heightSlider;
    [SerializeField] Transform mapRoot;
    [SerializeField] AbstractMap map;
    //private Transform parentTransform;

    void Start()
    {   
        map.OnUpdated += OnMapUpdated;

        if(heightSlider != null)
        {
            heightSlider.onValueChanged.AddListener(SetMapHeight);
        }
    }

    private void OnMapUpdated()
    {
        heightSlider.value = mapRoot.localPosition.y;
    }

    void SetMapHeight(float value)
    {
        mapRoot.localPosition = new Vector3(mapRoot.localPosition.x, value, mapRoot.localPosition.z);
        map.UpdateMap();
    }

    void Update()
    {
        heightSlider.value = mapRoot.localPosition.y;
    }*/

}
