using Mapbox.Examples.Voxels;
using Unity.XR.CoreUtils;
using UnityEngine;
using UnityEngine.UI;

public class rm_EmbeddedAnnotationsControl : MonoBehaviour
{
    /*[SerializeField] EmbeddedAnnotation Story1Annotation;
    [SerializeField] EmbeddedAnnotation Story2Annotation;
    [SerializeField] EmbeddedAnnotation Story3Annotation;
    [SerializeField] Toggle ShowAnnotation1;
    [SerializeField] Toggle ShowAnnotation2;
    [SerializeField] Toggle ShowAnnotation3;

    void Start()
    {
        ShowAnnotation1.onValueChanged.AddListener(Toggle1Changed);
        ShowAnnotation2.onValueChanged.AddListener(Toggle2Changed);
        ShowAnnotation3.onValueChanged.AddListener(Toggle3Changed);


    }

    void Toggle1Changed(bool b)
    {   
        Story1Annotation.SetVisibility(b);
        if(b) ToggleXChanged(1);
    }

    void Toggle2Changed(bool b)
    {   
        Story2Annotation.SetVisibility(b);
        if(b) ToggleXChanged(2);
    }

    void Toggle3Changed(bool b)
    {   
        Story3Annotation.SetVisibility(b);
        if(b) ToggleXChanged(3);
    }

    void ToggleXChanged(int t)
    {
        if(t == 1)
        {
            ShowAnnotation2.isOn = false;
            ShowAnnotation3.isOn = false; 
        }
        else if(t == 2)
        {
            ShowAnnotation1.isOn = false;
            ShowAnnotation3.isOn = false; 
        }
        else if(t == 3)
        {
            ShowAnnotation1.isOn = false; 
            ShowAnnotation2.isOn = false;
        }
    }*/

}
